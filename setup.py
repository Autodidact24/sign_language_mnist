from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='pipeline for ML experiments to classify Sign Language images',
    author='shubham',
    license='MIT',
)
